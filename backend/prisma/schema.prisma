model Event { id       Int     @id @default(autoincrement()) title    String   date     DateTime location String   description String? } model Rsvp { id       Int     @id @default(autoincrement()) eventId  Int     name     String   email    String   attendance Boolean guests   Int     event     Event    @relation(fields: [eventId], references: [id]) } model Photo { id       Int     @id @default(autoincrement()) url      String   eventId  Int     event     Event    @relation(fields: [eventId], references: [id]) }